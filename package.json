{
  "name": "todo",
  "version": "1.0.0",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "workspaces": [
    "api",
    "web",
    "libs/*",
    "services/*"
  ],
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
